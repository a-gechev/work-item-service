/**
 * The edge of the Users context.
 *
 * <p>Structure only; adapters live in the leaf packages below. Dependencies run inwards: an adapter
 * may depend on {@code application} and {@code domain}; neither may depend on an adapter.
 */
package io.github.agechev.workitem.users.impl.adapters;
