/**
 * Driven adapters: the ways the Users context calls out to infrastructure.
 *
 * <p>Structure only; the leaves below hold the code. Each implements a port declared in
 * {@code application}.
 */
package io.github.agechev.workitem.users.impl.adapters.out;
