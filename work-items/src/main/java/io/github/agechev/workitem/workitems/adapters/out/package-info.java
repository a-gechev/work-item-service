/**
 * Driven adapters: the ways this context calls out to infrastructure.
 *
 * <p>Structure only; the leaves below hold the code. Each implements a port declared in
 * {@code application}, so the direction of the source dependency is the opposite of the direction of
 * the call.
 */
package io.github.agechev.workitem.workitems.adapters.out;
