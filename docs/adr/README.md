# Architecture Decision Records

Short records of the decisions that shaped this project, each stating what was given up and
not only what was chosen. Populated as decisions are made:

- Modular monolith over microservices
- Outbox over direct event publish
- Testcontainers over H2
- PostgreSQL over MongoDB

Each ADR follows: context → decision → consequences → what was given up.
